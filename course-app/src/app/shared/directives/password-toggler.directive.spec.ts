import { PasswordTogglerDirective } from './password-toggler.directive';

describe('PasswordTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
